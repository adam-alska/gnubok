-- Placeholder: this migration was applied directly to the remote DB via the
-- Supabase MCP tool and never committed as a file. The file exists here so
-- the Supabase CLI sees it as already applied. No-op SQL — the real work
-- was a one-off backfill for the 'niklas' company's source_voucher column.
SELECT 1;
